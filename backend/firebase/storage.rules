rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /voice/{uid}/{file} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
    match /chat/{groupId}/{file} {
      allow read, write: if request.auth != null;
    }
  }
}
